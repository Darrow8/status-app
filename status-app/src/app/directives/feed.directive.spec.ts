import { FeedDirective } from './feed.directive';

describe('FeedDirective', () => {
  it('should create an instance', () => {
    const directive = new FeedDirective();
    expect(directive).toBeTruthy();
  });
});
